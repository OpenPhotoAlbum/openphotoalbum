export type AddSubjectExampleSuccess = {
  image_id: string;
  subject: string;
};

export type AddSubjectExampleFailure = {
  message: string;
  code: number;
};

// TODO: FINISH ME
export type SubjectType = {
  id: string;
};
